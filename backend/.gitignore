# Dependencias
node_modules/

# Build
dist/
build/
*.tsbuildinfo

# Variables de entorno (nunca subir secretos reales; usar .env.example como plantilla)
.env
.env.*.local
.env.local

# Contenido subido por usuarios (logos de iglesias, etc.) — no es código fuente
uploads/*
!uploads/.gitkeep
!uploads/logos/.gitkeep

# Prisma
prisma/*.db

# Tests / cobertura
coverage/

# Logs
*.log
npm-debug.log*

# IDE / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
prompt.md